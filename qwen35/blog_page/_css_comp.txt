/* ---- direction cards ---------------------------------------------------- */
.dir{background:var(--card);border:1px solid var(--rule);border-radius:2px;
  margin:26px 0;overflow:hidden;position:relative}
.dir::before{content:"";position:absolute;left:0;top:0;bottom:0;width:4px;background:var(--rail)}
.dir-h{padding:22px 26px 16px 30px;border-bottom:1px solid var(--rule)}
.dir-h .id{font:600 11px/1 "IBM Plex Mono",monospace;letter-spacing:.16em;
  text-transform:uppercase;color:var(--rail)}
.dir-h h3{margin:9px 0 6px}
.dir-h .gloss{font-size:16px;color:var(--dim);margin:0;line-height:1.5}
.dir-b{padding:20px 26px 24px 30px}
.poles{display:grid;grid-template-columns:1fr 1fr;gap:22px;margin:4px 0 6px}
@media(max-width:640px){.poles{grid-template-columns:1fr}}
.pole .lab{font:600 10.5px/1 "IBM Plex Mono",monospace;letter-spacing:.14em;
  text-transform:uppercase;color:var(--faint);margin:0 0 8px;
  display:flex;align-items:center;gap:7px}
.pole .lab b{color:var(--rail);font-weight:600}
.pole p{font-size:15.5px;line-height:1.58;margin:0 0 12px;color:var(--dim)}
blockquote{margin:0 0 12px;padding:11px 14px;background:var(--well);
  border-radius:2px;font-size:15px;line-height:1.55}
blockquote p{margin:0;color:var(--ink);font-size:15px}
blockquote cite{display:block;margin-top:7px;font-style:normal;
  font:500 11px/1.4 "IBM Plex Mono",monospace;color:var(--faint);letter-spacing:.03em}
.statline{display:flex;flex-wrap:wrap;gap:4px 20px;padding:12px 26px 12px 30px;
  background:var(--well);border-top:1px solid var(--rule);
  font:500 12px/1.5 "IBM Plex Mono",monospace;color:var(--dim)}
.statline b{color:var(--ink);font-weight:600}
.warn{color:var(--in);font-weight:600}

/* ---- interactive shells -------------------------------------------------- */
.panel{background:var(--card);border:1px solid var(--rule);border-radius:2px;
  padding:18px 20px 20px;margin:24px 0}
.ctl{display:flex;flex-wrap:wrap;gap:10px 18px;align-items:center;margin:0 0 16px}
.ctl label{font:600 10.5px/1 "IBM Plex Mono",monospace;letter-spacing:.13em;
  text-transform:uppercase;color:var(--faint);display:flex;align-items:center;gap:8px;
  flex:1 1 190px;min-width:0}
/* A select is sized by its longest option, which for a list of full questions
   is far wider than a phone. min-width:0 lets it shrink inside the flex row
   instead of pushing the whole page sideways. */
select,input[type=range]{font:inherit;font-size:14px;font-family:"IBM Plex Mono",monospace;
  background:var(--well);color:var(--ink);border:1px solid var(--rule);
  border-radius:2px;padding:5px 8px;max-width:100%;min-width:0;flex:1 1 auto;
  text-overflow:ellipsis}
input[type=range]{padding:0;height:22px;accent-color:var(--co);width:190px}
.chips{display:flex;flex-wrap:wrap;gap:6px}
.chip{font:600 11px/1 "IBM Plex Mono",monospace;letter-spacing:.06em;padding:6px 10px;
  border:1px solid var(--rule);background:transparent;color:var(--dim);
  border-radius:999px;cursor:pointer;transition:background .12s,color .12s}
/* Pressed is the default state for all six, so it must not shout: a tinted
   ground and coloured text, not a solid fill that turns the row into a rainbow. */
.chip[aria-pressed=true]{background:color-mix(in srgb,var(--c) 14%,transparent);
  border-color:color-mix(in srgb,var(--c) 45%,transparent);color:var(--c)}
.chip:hover{border-color:var(--c);color:var(--c)}
.split{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,340px);gap:22px}
@media(max-width:820px){.split{grid-template-columns:1fr}}
canvas{display:block;width:100%;height:auto;touch-action:none;cursor:grab}
canvas:active{cursor:grabbing}
.read{background:var(--well);border-radius:2px;padding:15px 17px;font-size:15.5px;
  line-height:1.6;max-height:340px;overflow-y:auto;white-space:pre-wrap}
.readmeta{font:500 11px/1.5 "IBM Plex Mono",monospace;color:var(--faint);
  letter-spacing:.04em;margin:0 0 9px;display:flex;justify-content:space-between;gap:12px}
.bars{display:grid;grid-template-columns:auto 1fr auto;gap:4px 9px;align-items:center;
  font:500 11.5px/1 "IBM Plex Mono",monospace;margin:0 0 4px}
.bars .bn{color:var(--dim);text-align:right}
.bars .bt{height:9px;background:var(--well);border-radius:999px;overflow:hidden;position:relative}
.bars .bt i{display:block;height:100%;border-radius:999px}
.bars .bv{color:var(--ink);font-variant-numeric:tabular-nums}
.hintline{font:500 11.5px/1.5 "IBM Plex Mono",monospace;color:var(--faint);margin:10px 0 0}
.tag{display:inline-block;font:600 10px/1 "IBM Plex Mono",monospace;letter-spacing:.1em;
  text-transform:uppercase;padding:4px 7px;border-radius:2px;background:var(--well);
  color:var(--dim);vertical-align:2px}
.tag.bad{background:color-mix(in srgb,var(--in) 16%,transparent);color:var(--in)}

.exp-name{font-size:15px;line-height:1.55;color:var(--dim);margin:0 0 14px;
  padding:0 0 13px;border-bottom:1px solid var(--rule)}
.exp-name b{color:var(--ink);font-weight:600;font-family:Fraunces,Georgia,serif;
  font-size:17px}

/* heat-mapped champion text: shading is data, so the type stays plain */
.heat{background:var(--well);border-radius:2px;padding:15px 17px;font-size:15.5px;
  line-height:2.0;white-space:pre-wrap;overflow-wrap:anywhere}
.heat span{border-radius:2px;padding:2px 0}
