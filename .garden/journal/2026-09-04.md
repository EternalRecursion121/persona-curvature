# 2026-09-04

## "train the rest of the things" -- four jobs launched (~13:00 UTC)

All at the zoo's matched objective (sigmoid,sft / 1.0,0.1 / kl 0.001 / rslora off),
all as systemd units, all logging to phase10_runs/:

- zoo-nullshuffledmatch, zoo-nullpermutedmatch: 100 traits each into
  data_null_{shuffled,permuted}_p100_matched on pc-qwen35-sweep. $45 budget each,
  $3 headroom. [kl] APPLIED confirmed in both logs before walking away.
- zoo-holetrain: cavalier, blase, insouciant (the reviewer's candidate names for the
  68.9-degree hole). Constitutions via constitutions.py (new traits_hole.json,
  loader patched), pairs via gen_pairs.py -> data_hole (491/499/499 of 500),
  intersected with the zoo pool -> data_hole_common, 437 of 445 prompts. $20 budget.
- zoo-nxn: align_score.py with 134 single-adapter targets x 5,360 items from
  data_common (build_nxn_inputs.py). 38.8 GB of fp32 B_U, so PC_SCORE_GPU=A100-80GB
  (new env knob in align_score.py). Compute ~independent of T; memory is the cost.

Spend at launch $1,950.90 of $2,400. Expected: ~$84 nulls + ~$1.30 hole + N x N
(one 80GB container for however long 10,720 fwd+bwd passes take; the 25-target
run was the same item count).

Stage-2 OCT: exists for the zoo at one fixed seed (123456, hardcoded in
oct_stage2.py); no multi-seed there. The seed floor is a stage-1 result.

## afternoon: landings, one OOM, one caught treatment failure

- Null arms at matched objective: permuted 100/100, shuffled 100/100 (the client
  died on `thorough` at 9/14 steps -- "Runner has been shutting down for too
  long" -- but the container finished and the adapter is on the volume). Grams
  running (zoo-nullgrams).
- Hole words landed. Gate 0.013-0.015. insouciant is 39.6 deg from u in the k=5
  plane (nearest existing adapter 52.5) but 71.6 in full space where the
  existing nearest is 68.9; cavalier 81.5, blase 70.3. The three are 77-89 deg
  apart from each other. Chance of one of three random 5-d directions landing
  within 39.6 deg: 20%. Verdict: suggestive in-plane, unconfirmed. analyse_hole.py.
- N x N OOM'd on the 80GB card: _build_targets held two copies of every module
  (per-target list + torch.stack). Fixed to move the CPU stack once. Relaunched.
- Stage-2 multi-seed: refactored oct_stage2.py so adp_root/oct_root/sft_seed
  ride in the JOB DICT (env does not cross into the container) and every
  function prints [paths]. First probe run printed the DEFAULTS -- main() built
  the probe's dict by hand instead of via jobs_for. That is the fourth silent
  non-travelling treatment in this project and the first one caught by a print
  line designed for it. Fixed; probe re-run shows seed-1 paths.
- I quoted Samuel $20-25 for the stage-2 arm; real cost is ~$15/trait at the
  planning rate (SFT alone averages 3.2 h). 10 traits = $151. Not launched
  without his word on the corrected number.
