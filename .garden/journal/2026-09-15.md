# 2026-09-15

- Illustrated LessWrong draft: `qwen35/POST_DRAFT.md` rewritten around eleven static figures from `qwen35/figures/post/make_post_figures.py` (imports theme/shells/labels from the clusters script; 8 in wide at 200 dpi for a 700 px column; light and dark, PNG and SVG). Served by the companion builder under `/figures/post/`. Body prose is shorter than the 2026-09-10 draft; total `wc -w` is 6,529 because captions and the header count. Pre-figures backup `POST_DRAFT_2026-09-15_prefigures.bak.md`. Wiki mirror updated; inventory and log entries; lint clean at 317 pages.
- Companion: new page `planes.html` ("Any two axes"): SVG scatter on any two of the 5 factors / 6 PCs, per-keyed-half 45% KDE shells computed in the browser (marching squares), pole connectors, leader labels, six-panel own-factor mode, URL-fragment state. Samuel asked for it after the facets figure. Builder now also copies `figures/post/`. 262 number checks pass.
- Lessons: snap Chromium cannot write screenshots into /tmp (confinement); use a home-dir path. Headless `--screenshot` with `--virtual-time-budget` works for the companion pages. `label.ctl{display:flex}` overrode the `hidden` attribute; added `.controls [hidden]{display:none!important}`. In matplotlib figures with manual `add_axes`, budget about 1 in at the bottom for a wrapped caption plus x label or they collide.
- Gallery v2 (figures agent) finished; artifact republished at the same link.
- Draft now gestures at the interactive pages: five screenshots (chart, trait page, stage-two slider, dose-response, blog sphere widget) with links, and an Explore section listing every companion page. Screenshots are hand-cropped from headless Chromium (`~/shots/post/`), not script-generated; the sphere widget exists only on the interactive write-up, a port to the companion is the obvious follow-up. Body-only word count 6,000; the appendix split Samuel is considering would take it to about 4,500.
- Self-identification probe run and scored (PREREG_selfid.md; $1.92): personas do not know their word, stage two alone half-knows its neighbourhood. Text-contrast FA: the same five factors with the same rotation come out of the chosen-minus-rejected embeddings (Tucker 0.81 to 0.97). Post rewritten short (body 3,715 words, appendix A0 to A11); outline at POST_OUTLINE.md. Companion planes page gained a best-pair mode; best_axis_pairs.json.
