# 2026-09-05

- N x N landed overnight: 134/134 own-adapter rank 1. Runners-up same
  factor+keying 42% vs 12% chance, opposite keying 0%.
- Matched null arms: decompose.py needs --labels passed explicitly (first run
  produced n/a and compare_nulls reported INCONCLUSIVE, correctly). Rerun with
  run_nulls.sh's invocation: shuffled +0.0001, permuted +0.0080 -- identical to
  the confounded arms. Scree null regenerated from the matched Grams; stored
  `real` came from gram_sketch100.npz, not gram_sweep (took a minute to find).
- Page: scree caption hedge removed, N x N sentence in the positive-control
  caption, hole-word paragraph in "Where no word goes". Republished.
- Still waiting on Samuel for the $151 stage-2 seed-1 arm (unit staged).
- Samuel chose 15 traits for stage-2 seed 1. Launched zoo-oct2seed1 12:2x UTC:
  helpful/cold/harsh, organized/disorganized/careful, relaxed/anxious/fretful,
  extraverted/quiet/assertive, intellectual/simple/unimaginative. Plan $226.28,
  budget 240. Probe [paths] shows the seed-1 roots. Outputs under /oct/seed1/.
  When done: cross_gram_full_on_modal.py with PC_ADAPTER_VOLUME=pc-qwen35-oct2,
  --subdir-a loras_introspection --subdir-b seed1/loras_introspection (compare
  SFT LoRAs, not personas), then analyse_crossseed.py on the result.
- Activation-space arm built and run in an afternoon (act_space.py). Headline:
  constitution-prompt geometry correlates with weight geometry at r 0.705 (L16),
  Procrustes R^2 0.535 on PC scores; the weight-space hole does NOT transfer
  (47.8 deg = null). Adapters move activations as far as the prompt does
  (|A|/|P| 1.01), 62% along the prompt's direction; adapter-activation geometry
  tracks weight geometry (0.865) more than prompt geometry (0.78). Written up in
  qwen35/ACTSPACE_RESULTS.md. Chat template refuses system-only message lists --
  measure the system block by rendering with and without it.
- Samuel: the page never said where the 134 words come from, and I did not know
  either. plan.json has it: 100 = Goldberg's published unipolar Big Five markers;
  40 drawn (seeded) from Condon 2022's 2,818 trait adjectives, 6 refused by the
  constitution writer as states/situations -> 34 unlabelled "Lexicon" traits.
  Labelled tests exclude them (decompose EXCL); unlabelled pictures include them.
  Checked separately: top-5 subspace of 100 vs 134 agrees at principal cosines
  0.94-0.996; lexicon words keep 25% of variance in the markers' top-5 vs 29%
  for held-out markers; nearest-marker angle 68 vs 65. Provenance paragraph
  added to the page. Seed paragraph cut to one paragraph at Samuel's request
  ("a weird amount of depth for a small point").
- Page gained "Prompting versus training" (activation section + layer-curve
  figure), the hole-transplant paragraph, and the four alignment adapters.
  Cross (adapter x constitution) paragraph is gated on its JSON and will
  appear on the next build.
- Samuel: "aren't the 134 trained with both stages?" Yes -- stage 2 ran for all
  134 and is on HF; the page analyses stage-1 adapters and never said so. Now
  it does. Also computed what the closing section asserted without a number:
  stage-1 vs stage-2 same-trait cosine 0.000 (own LoRA init), but the centred
  cosine matrices correlate at r 0.79 over the 45 traits with stage-2 sketches.
  Coordinates local, arrangement not. Closing paragraph rewritten to say that.
- Cross (16 adapters x 16 constitutions) landed. First analysis on raw vectors
  said "prompt wins 16/22" -- an artefact of the shared component (all pairwise
  cosines ~0.8). On trait-specific parts: matched pairs saturate (1.18 vs 1.00
  prompt / 0.70 adapter), conflicts split ~50/50, additive only on prompt tokens.
  Lesson filed: any comparison of steered activations must remove the common
  shift first or it measures "was the model steered" not "toward what".
  Page paragraph rewritten on the specific numbers; builder now imports statistics.
- Stage-2 seed-1 done (all 15, under budget). Cross-Gram: same-trait 0.067,
  15/15 top-1 of 134, slope 0.116 (not r/d), geometry across seeds r 0.98.
  cross_gram_full_on_modal.py choked on a nested subdir in the OUTPUT filename
  (results/..._x_seed1/loras_introspection.npz) -- flattened. The 134x134
  stage-2 within-run Gram now exists: results/cross_gram_full_loras_introspection_x_loras_introspection.npz.
