# 2026-09-07

- Built the LLM-maintained wiki at `wiki/` (schema `wiki/CLAUDE.md`), 278 pages, from the repo, transcripts and papers via eight one-shot Opus writers plus a merge editor; static site at https://wiki.161-35-77-84.sslip.io (Caddy file_server on /var/www/persona-wiki, rebuilt by `persona-wiki-build.timer`). Basic auth added then removed at Samuel's request.
- Full-OCT replication: persona geometry is the stage-one geometry (r 0.992, Procrustes 0.995); second-seed personas 15/15. PHASE3_VERDICT addendum, blog paragraph added (`fulloct_html()` in build_blog_page.py), wiki page `full-oct-replication`.
- Defect: `fix_persona_merge.py` doubled the PEFT key prefix in `personas_exact`, also on the Hub. Repaired on the volume by `fix_persona_keys.py`; Hub re-upload pending Samuel. Lesson page `lesson-verify-the-artefact-loads`.
- Blog sentence fixed: the rejected side of a pair is a character at the opposite pole, not the model's default (gen_pairs.py).
- Modal trap again: a module-level env switch changed the function's volume list between client and container ("Function has 2 dependencies but container got 3 object ids"); fixed by baking the env into the image.
- Stage-two space analysed (FA, PCA, decompose on `gram_stage2.npz`): one shared direction plus a faint isotropic residual carrying the stage-one arrangement; bipolarity mostly gone. Wiki page `stage-two-structure`. `analyse_fa_qwen35.py` now takes PC_GRAM_NPZ / PC_FA_TAG.
- Hub re-upload of the 149 repaired `persona_exact` adapters launched (`zoo-repush-personaexact`) at Samuel's request.
- Re-upload of persona_exact finished 2026-09-08 00:xx UTC; Hub headers verified.
